@model List<Patisserie.Models.Words>

<section class="section section-xl bg-default">
    <div class="container">
        <h3 class="wow fadeInLeft">Sözler</h3>
    </div>
    <div class="container container-style-1">
        <div class="owl-carousel owl-style-12" data-items="1" data-sm-items="2" data-lg-items="3" data-margin="30" data-xl-margin="45" data-autoplay="true" data-nav="true" data-center="true" data-smart-speed="400">
            <!-- Quote Tara-->

            @foreach (var item in Model)
            {
                String a = "sozText" + item.Id.ToString()+".jpg";
                <article class="quote-tara">
                    <div class="quote-tara-caption">
                        <div class="quote-tara-text">
                            <p class="q">@item.Text</p>
                        </div>
                        <div class="quote-tara-figure">
                            <img src="~/assets/images/@a" alt="" width="115" height="115" />
                        </div>
                    </div>
                    <h6 class="quote-tara-author">@item.Name</h6>
                    
                </article>
            }

        </div>
    </div>
</section>